import {Address, TokenAmount} from "../../core/types/types";
import {LotusClient} from "filecoin.js";

export class Msig {
    createMultisig(sender: Address, addresses: Address[], value: TokenAmount,
                   requiredNumberOfSenders: number, nonce: number, duration: string, startEpoch: string) {


    }
}
